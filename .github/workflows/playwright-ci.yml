name: Playwright BDD Parallel Matrix Suite

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# Global environment variables to force Node 24 runtime compliance
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test-matrix:
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false 
      matrix:
        test-type: [ui, api] 

    name: Execute ${{ matrix.test-type }} Tests
    steps:
      # Upgraded to v5 (Natively compiled for Node 24 runtime support)
      - name: Checkout Repository
        uses: actions/checkout@v5

      # Upgraded to v6 (Natively compiled for Node 24 runtime support)
      - name: Set up Python Engine
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Pinned Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright System Binaries (UI Node Only)
        if: matrix.test-type == 'ui'
        run: |
          playwright install --with-deps chromium

      - name: Run Matrix Test Subsets
        run: |
          if [ "${{ matrix.test-type }}" = "ui" ]; then
            pytest -m ui -s -n 2
          else
            pytest -m api -s
          fi

      # Upgraded to v6 (Natively compiled for Node 24 runtime support)
      - name: Upload Test Report (${{ matrix.test-type }})
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: pytest-html-report-${{ matrix.test-type }}
          path: report.html
          if-no-files-found: ignore
